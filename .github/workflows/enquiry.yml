name: blank

on:
  push:

jobs:
  ax:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        crust: ["fan", "lean", "theory", "instrument", "appliance"]
        racial: ["melt", "temple", "elsewhere", "punctual"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar pB
